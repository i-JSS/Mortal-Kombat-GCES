stages:
  - build

build:
  stage: build
  image: docker:latest
  services:
    - docker:latest-dind
  variables:
    DOCKER_HOST: tcp://docker:2375/
    DOCKER_TLS_CERTDIR: ""
  script:
    - docker compose -f docker-compose.yml build